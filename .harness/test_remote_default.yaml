version: 1
name: test_remote_default
stages:
  - name: build
    type: ci
    spec:
      steps:
        - name: Run echo
          type: script
          spec:
            run: echo "Hello Harness CI!"
repository:
  connector: account.GithubAccountConnector
